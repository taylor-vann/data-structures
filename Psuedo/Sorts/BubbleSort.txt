bubbleSort(list)
  l as length of list

  if l < 2 return list

  swap = True

  while swap is True
    for j in range 0 to l - 1
      if l[j] > l[j + 1]
        swap j and j + 1
        swap = False

    if swap is True break while loop

    swap is True

  return list
